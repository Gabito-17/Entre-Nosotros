import React from "react";
import "./Title.css"; // Asegúrate de tener un archivo CSS para los estilos del título

const Title = () => {
  return <div className="text-gradient font-bold text-large">Britney</div>;
};

export default Title;
